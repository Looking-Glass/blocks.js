query MyHolograms($first: Int = 20) {
	me {
		username
		displayName

		holograms(first: $first) {
			nodes {
				id
				title
				aspectRatio
				quiltCols
				quiltRows
				quiltTileCount
				sourceImages {
					id
					url
					width
					height
					type
					fileSize
				}
			}
		}
	}
}
